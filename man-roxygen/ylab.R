#' Value for y axis label.
#' This argument maps to the yaxis layout object in plotly. 
#' Use the plotly `plotly::schema()` command in the console and navigate
#' to layout > layoutAttributes > yaxis to see other ways to customize
#' this axis using lists as additional arguments to the plot function.
#' Example: `yaxis = list(title = list(text = "Concentration", font = list(color = "blue", family = "Times New Roman", size = 12)))`
