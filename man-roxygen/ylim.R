#' Limits of the y axis as a vector.  
#' This argument maps to the  
#' the yaxis range in the layout object in plotly. 
#' Use the plotly `plotly::schema()` command in the console and navigate
#' to layout > layoutAttributes > yaxis > range.<br>
#' <br>
#' Example: `ylim = c(0,100)`
