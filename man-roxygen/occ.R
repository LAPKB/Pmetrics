#' Which observation occasion to use, where occasions are defined
#' by dose reset events (`EVID = 4`) in the data. The underlying occasion
#' identifier in Pmetrics data objects remains stored in the `block` column.
