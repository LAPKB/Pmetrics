#' @param \dots Other parameters which can be passed to control axis formatting. Specify as lists
#' named either xaxis or yaxis. See `plotly::schema()` layout > layoutAttributes > xaxis/yaxis for options.
#' Example: `xaxis = list(title = list(text = "Time (h)", font = list(family = "Arial", size = 14, color = "black")))`
