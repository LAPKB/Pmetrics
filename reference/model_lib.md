# Model Library

Returns a table of all available model templates in the Pmetrics
library.

## Usage

``` r
model_lib(show = TRUE)
```

## Arguments

- show:

  Logical indicating if the table should be printed. Default is TRUE.

## Value

Invisibly, a tibble containing the model templates or a specific model
template.
